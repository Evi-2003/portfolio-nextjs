'use strict';

const kit = require('@nuxt/kit');
const viteCompression = require('vite-plugin-compression');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e["default"] : e; }

const viteCompression__default = /*#__PURE__*/_interopDefaultLegacy(viteCompression);

const module$1 = kit.defineNuxtModule({
  name: "@nuxt-modules/compression",
  configKey: "compression",
  setup(options, nuxt) {
    nuxt.hook("vite:extendConfig", (clientConfig) => {
      clientConfig.plugins.push(viteCompression__default(options));
    });
  }
});

module.exports = module$1;
